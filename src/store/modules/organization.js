// import axios from 'axios';

const state = {
  organization: [],
};

const getters = {
  allOrganizations: (currentState) => currentState.organization,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
